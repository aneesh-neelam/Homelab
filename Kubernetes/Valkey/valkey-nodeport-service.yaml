apiVersion: v1
kind: Service
metadata:
  name: valkey-nodeport
  namespace: valkey
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: valkey
  ports:
    - name: valkey
      protocol: TCP
      port: 6379
      targetPort: 6379
      nodePort: 32379
